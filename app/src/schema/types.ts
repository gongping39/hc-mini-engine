// Re-export GameSpec type from validator for consistency
export type { GameSpec, ValidationResult } from './validator';

// Legacy export for backward compatibility
export type { GameSpec as GameSpecLegacy } from './validator';